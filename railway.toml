[build]
builder = "nixpacks"

[build.nixpacks]
python = "3.11"

[[services]]
name = "web"
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "worker"
startCommand = "python worker.py"
